{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "skia:web"],
      "env": ["VITE_API_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"],
      "outputs": ["dist/**"]
    }
  }
}
