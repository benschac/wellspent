# Copy to .env.prod-api.local. This profile never reads auth from .env.local.
# Use the SAME hosted Supabase project trusted by api.wellspent.day.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# API URLs and port 3002 are supplied by dev:prod-api.
# No Google client secrets, encryption keys, or service-role keys belong here.
