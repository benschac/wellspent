# Supabase: replace this with the Shared Pooler URI from the project's Connect panel.
# URL-encode special characters in the database password.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/timer
