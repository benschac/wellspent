# Learning more about Effect

This package uses the Effect TypeScript library.

Before writing any Effect code, first read `node_modules/effect/AGENTS.md`
completely (relative to this package), and follow the links when required.

For APIs and concepts not covered there, search `node_modules/effect/src`.
